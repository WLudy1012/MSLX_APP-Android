# 二进制产物：禁止任何换行符转换。
# 维护者机器上 core.autocrlf=true，若 git 把这些文件误判为文本，checkout 时会改写字节 ——
# 内嵌 JRE 归档会直接解不开、LD_PRELOAD 垫片会加载失败（且只在克隆端复现，最难查）。
*.so binary
*.aar binary
*.jar binary
*.apk binary
*.jks binary
*.xz binary
*.zip binary
*.gz binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.ttf binary

# 脚本与配置统一 LF，避免 CI 里的 shell 步骤被 CRLF 干扰
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Actions 调试构建渠道：应用内下载 APK 并拉起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 本机开服：前台服务保活（常驻通知），避免退到后台/任务被划掉时服务端掉线 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 本机开服增强：Shizuku（ADB/root 权限）下 exec 真正的 java 子进程 -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <!--
        本机开服「公共目录实例」：Android 11+ 的 All Files Access（仅用于 /storage/emulated/0/MSLX，
        让用户能在系统文件管理器里直接看到并编辑服务端文件）。不会用于读取用户其它私人文件，
        也未授权时一切功能仍按应用私有目录正常工作。
    -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!--
        usesCleartextTraffic=true：系统层允许明文，但业务层默认仍强制 HTTPS
        （未勾选"允许 HTTP"时 http 地址自动升级为 https）。
        仅当用户在连接页勾选"允许 HTTP"并确认 5 秒警告后才走明文连接（风险自担）。
    -->
    <application
        android:name=".MSLXApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MSLXConsole"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MSLXConsole.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.connect.PairingScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MSLXConsole.Scanner"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- 本机开服保活：前台服务只负责常驻通知与进程存活，服务端实例在 LocalServerRuntime -->
        <service
            android:name=".data.localengine.LocalServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep the in-process Minecraft server (JVM) alive while hosting on device" />
        </service>

        <!-- Actions 渠道 APK 下载到 filesDir/apks/ 后，经 FileProvider 提供给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Shizuku：应用启动时经此 provider 与 Shizuku 服务绑定（本机开服增强模式 exec java） -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>

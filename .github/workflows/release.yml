name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Build & attach signed APK
    runs-on: ubuntu-latest
    env:
      # ubuntu runner 自带 SDK 的固定路径；缓存 NDK 等组件时需要以 env 形式引用
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # 完整克隆以获取历史提交与 tag
          fetch-depth: 0
          # 必须显式取 tag：否则 git tag --list 为空 → 找不到"上一版本"，
          # release notes 会退化成"把以往所有提交都列出来"
          fetch-tags: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      # Android SDK 组件缓存：NDK（约 1GB）是最大耗时项，发版频率低更要缓存。
      # 缓存 miss 时 AGP 会自动下载并在 job 结束时由本 step 回写缓存。
      # 注意：Gradle 依赖缓存（~/.gradle）由下面的 gradle/actions/setup-gradle 负责，
      # 这里不要重复缓存 ~/.gradle，避免两套缓存还原到同一目录互相覆盖。
      - name: Cache Android SDK (NDK / platform / build-tools)
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.ANDROID_SDK_ROOT }}/ndk/28.2.13676358
            ${{ env.ANDROID_SDK_ROOT }}/platforms/android-35
            ${{ env.ANDROID_SDK_ROOT }}/build-tools/35.0.0
          # key 写入组件版本：升级 NDK/SDK 时 key 自然失效，缓存随之重建
          key: android-sdk-35-ndk-28.2.13676358-${{ runner.os }}-${{ runner.arch }}
          restore-keys: |
            android-sdk-35-ndk-28.2.13676358-${{ runner.os }}-

      # 不用 android-actions/setup-android@v3：它在末尾会执行 `sdkmanager tools`，而 Google
      # 已把 `tools` 包从仓库下架（Warning: Failed to find package 'tools' → exit 1），
      # runner 镜像换成 cmdline-tools 16.0 后这一步必然失败（v1.6.6-Beta 起 Release 全挂）。
      # ubuntu 运行器自带 Android SDK（ANDROID_HOME）且许可已在镜像内接受，AGP 会按需补齐
      # 缺失的 platform / build-tools，所以下面只做容错预热，任何失败都不阻断发布。
      - name: Set up Android SDK
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          SDKMAN=""
          for c in "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager; do
            if [ -x "$c" ]; then SDKMAN="$c"; break; fi
          done
          if [ -z "$SDKMAN" ]; then
            echo "::warning::未找到 sdkmanager，直接使用镜像预装 SDK"
            exit 0
          fi
          yes | "$SDKMAN" --licenses > /dev/null 2>&1 || true
          "$SDKMAN" "platforms;android-35" "build-tools;35.0.0" platform-tools \
            || echo "::warning::sdkmanager 预装未完全成功，交由 AGP 自动下载"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # 依赖缓存健康检查（非阻断）：缓存完整时依赖解析可零网络通过；
      # 首次发布或依赖有更新时失败，仅提示正式构建会联网补齐。
      - name: Verify dependency cache (offline dry-run)
        run: |
          chmod +x gradlew
          if ./gradlew :app:dependencies --offline -q > /dev/null 2>&1; then
            echo "依赖缓存完整：offline 解析通过（install 阶段可零网络完成）"
          else
            echo "::warning::offline 解析未完全通过（首次构建或依赖已更新），正式构建将联网补齐"
          fi

      # 本机开服把 Android JRE 内嵌进 APK：仓库内归档必须存在且哈希正确，
      # 否则发布出去的包会退化成"首次开服需联网下载运行时"。
      - name: Verify embedded JRE runtime
        run: |
          set -e
          JRE=app/src/jreRuntime/assets/jre/jre17-arm64-20210825-release.tar.xz
          SHA=c64583ac2e0ec8857e43456fa9adcf482c6a8e454a7133173bf15692d2478b8d
          if [ ! -f "$JRE" ]; then
            echo "::error::缺少内嵌 JRE 归档 $JRE（本机开服的运行时）"
            exit 1
          fi
          echo "$SHA  $JRE" | sha256sum -c -
          ls -lh "$JRE"

      # Java 21/25 运行时不入库、不进 APK，只在发版时从上游
      # 镜像抓一份挂成 Release 附件。GitHub 侧走“尽力而为”：失败只告警不阻断发布
      # （下面 files 对不存在的文件会自动跳过），CNB 流水线那边才是硬校验。
      - name: Fetch JRE 21 runtime archive
        run: |
          JRE21=jre21-arm64-20260223-release.tar.xz
          SHA=d055fc953771e6cfd2206ef770ca675d69b5e4cfd9f3f1e6820ba5820c3cc1dc
          URL="https://pan.huang1111.cn/f/eWkQI1/$JRE21"
          if curl -fL --retry 3 --max-time 900 -o "$JRE21" "$URL" && echo "$SHA  $JRE21" | sha256sum -c -; then
            ls -lh "$JRE21"
          else
            rm -f "$JRE21"
            echo "::warning::获取 jre21 归档失败，本次 Release 将不含该附件（客户端会回退到 FCL 直链）"
          fi

      # 客户端已支持 Java 25；同步发布 GitHub 镜像，避免该下载来源固定返回 404。
      - name: Fetch JRE 25 runtime archive
        run: |
          JRE25=jre25-arm64-20260223-release.tar.xz
          SHA=0fdf6d19fe66ea61c12caa24bd655227ddb0d7d9c16c0f13281a7c2878635286
          URL="https://pan.huang1111.cn/f/5NOltm/$JRE25"
          if curl -fL --retry 3 --max-time 900 -o "$JRE25" "$URL" && echo "$SHA  $JRE25" | sha256sum -c -; then
            ls -lh "$JRE25"
          else
            rm -f "$JRE25"
            echo "::warning::获取 jre25 归档失败，本次 Release 将不含该附件（客户端会回退到 FCL 直链）"
          fi

      # 从仓库 Secrets 恢复签名密钥并生成 keystore.properties。
      # 需在仓库 Settings → Secrets and variables → Actions 配置：
      #   KEYSTORE_BASE64   = base64 编码后的 .jks 文件内容
      #   KEYSTORE_PASSWORD = 密钥库密码
      #   KEY_ALIAS         = 密钥别名
      #   KEY_PASSWORD      = 密钥密码
      - name: Restore signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          set -e
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::error::Secret KEYSTORE_BASE64 is empty. 请在仓库 Settings → Secrets and variables → Actions 配置该 Secret（值见本地 ACTIONS_GUIDE.md）。"
            exit 1
          fi
          if [ -z "$KEYSTORE_PASSWORD" ] || [ -z "$KEY_ALIAS" ] || [ -z "$KEY_PASSWORD" ]; then
            echo "::error::Secret KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD 不能为空。"
            exit 1
          fi
          mkdir -p keystore
          # 校验 base64 值是否合法（必须是单行、无空格/换行/表格符号的纯 base64）
          echo "$KEYSTORE_BASE64" | tr -d '[:space:]' | base64 --decode > keystore/release.jks || {
            echo "::error::KEYSTORE_BASE64 解码失败。请从 ACTIONS_GUIDE.md 的代码块中整行复制纯 base64 值（不要带空格或 | 符号），重新配置该 Secret。"
            exit 1
          }
          cat > keystore.properties <<EOF
          storeFile=keystore/release.jks
          storePassword=$KEYSTORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEY_PASSWORD
          EOF

      # 同一 Release 发布两个变体：
      #   app-release.apk      完整版（内嵌 JRE 运行时，可离线开服）
      #   app-release-lite.apk 精简版（不含运行时，体积小；本机开服首次使用需联网下载运行时）
      # 并把 JRE 归档本身作为资产挂上，供精简版在应用内下载（客户端按 CNB → GitHub 顺序尝试）。
      - name: Build signed release APK (full, embedded JRE)
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon --stacktrace
          cp app/build/outputs/apk/release/app-release.apk ./app-release.apk

      - name: Build signed release APK (lite, no JRE)
        run: |
          ./gradlew assembleRelease -PwithoutJre=true --no-daemon --stacktrace
          cp app/build/outputs/apk/release/app-release.apk ./app-release-lite.apk
          ls -lh app-release.apk app-release-lite.apk
          # 自检：完整版必须含内嵌 JRE，精简版必须不含
          unzip -l app-release.apk | grep -q 'assets/jre/' || { echo "::error::完整版缺少内嵌 JRE"; exit 1; }
          if unzip -l app-release-lite.apk | grep -q 'assets/jre/'; then
            echo "::error::精简版不应包含内嵌 JRE"
            exit 1
          fi

      # 生成 release notes：取"上一版"并列出其到本版的全部提交。
      # 层级规则（正式版 x.x / Beta x.x.x / Actions x.x.x.x）：
      #   - 正式版/强制版：上一版 = 最近一个不带 -Beta 后缀的 v* tag（跳过中间所有 Beta，保证
      #     release note 包含"直至上一正式版的全部 Beta 更新"）；
      #   - Beta 版：上一版 = 最近一个带 -Beta 后缀的 v* tag（包含中间 Actions 更新）；若无，
      #     回退到最近一个不带 -Beta 的 tag。
      - name: Generate release notes
        run: |
          set -e
          CUR_TAG="${GITHUB_REF_NAME:-$(git describe --tags --exact-match)}"
          # 双保险：显式同步远端 tag（checkout 已带 fetch-tags，这里再兜一次）
          git fetch --tags --force --quiet || true
          echo "当前 tag: $CUR_TAG；可见 v* tag 数量: $(git tag --list 'v*' | wc -l)"
          IS_BETA=$(echo "$CUR_TAG" | grep -qiE -- '-beta$' && echo 1 || echo 0)
          if [ "$IS_BETA" = "1" ]; then
            PREV_TAG=$(git tag --list 'v*' | grep -iE -- '-beta$' | grep -v "^${CUR_TAG}$" | sort -V | tail -1)
            if [ -z "$PREV_TAG" ]; then
              PREV_TAG=$(git tag --list 'v*' | grep -ivE -- '-beta$' | grep -v "^${CUR_TAG}$" | sort -V | tail -1)
            fi
          else
            PREV_TAG=$(git tag --list 'v*' | grep -ivE -- '-beta$' | grep -v "^${CUR_TAG}$" | sort -V | tail -1)
          fi
          echo "上一版本 tag: ${PREV_TAG:-<无>}"
          {
            echo "# MSLX_APP-Android $CUR_TAG"
            echo ""
            if [ -n "$PREV_TAG" ]; then
              echo "自 \`$PREV_TAG\` 以来的提交："
              echo ""
              git log --no-merges --pretty=format:"- %s" "${PREV_TAG}..${CUR_TAG}"
            else
              # 绝不回退成"列出全部历史提交"：只给最近若干条并说明原因
              echo "> 未能确定上一版本 tag（首个版本或 tag 未同步），仅列出最近 15 条提交。"
              echo ""
              git log -n 15 --no-merges --pretty=format:"- %s" "${CUR_TAG}"
            fi
            echo ""
            echo ""
            echo "> 上一版本：\`${PREV_TAG:-无}\`"
          } > release_notes.md

      # 计算两个变体的 SHA256 并写入 release notes，供用户校验文件完整性
      - name: Compute APK SHA256
        run: |
          FULL_SHA=$(sha256sum app-release.apk | awk '{print $1}')
          LITE_SHA=$(sha256sum app-release-lite.apk | awk '{print $1}')
          FULL_MB=$(awk "BEGIN{printf \"%.1f\", $(stat -c%s app-release.apk)/1048576}")
          LITE_MB=$(awk "BEGIN{printf \"%.1f\", $(stat -c%s app-release-lite.apk)/1048576}")
          {
            echo ""
            echo "## 两个变体（按需选择）"
            echo ""
            echo "| 包 | 说明 | 大小 | SHA-256 |"
            echo "| --- | --- | --- | --- |"
            echo "| \`app-release.apk\` | **完整版**：内嵌 Android JRE 运行时，本机开服可离线使用 | ${FULL_MB} MB | \`$FULL_SHA\` |"
            echo "| \`app-release-lite.apk\` | **精简版**：不含运行时，体积小；本机开服首次使用需联网下载约 36MB 运行时 | ${LITE_MB} MB | \`$LITE_SHA\` |"
            echo ""
            echo "附件里的 \`jre17-*.tar.xz\` / \`jre21-*.tar.xz\` / \`jre25-*.tar.xz\` 是本机开服用的 Android 运行时归档（应用内自动拉取，无需手动安装）。"
            echo ""
            echo "两个包版本号与签名一致，可互相覆盖安装（切换变体无需卸载）。"
            echo ""
            echo "校验命令（Windows PowerShell）："
            echo "\`\`\`powershell"
            echo "Get-FileHash app-release.apk -Algorithm SHA256"
            echo "Get-FileHash app-release-lite.apk -Algorithm SHA256"
            echo "\`\`\`"
          } >> release_notes.md

      - name: Upload APKs to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app-release.apk
            app-release-lite.apk
            app/src/jreRuntime/assets/jre/jre17-arm64-20210825-release.tar.xz
            jre21-arm64-20260223-release.tar.xz
            jre25-arm64-20260223-release.tar.xz
          body_path: release_notes.md
